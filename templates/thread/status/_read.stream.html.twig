<turbo-stream action="replace" target="thread_{{ message is defined ? message.id : thread.id }}"{{ message is defined ? message.id : thread.id }}">
    <template>
        {% include '_partials/_thread_row.html.twig' %}
    </template>
</turbo-stream>
