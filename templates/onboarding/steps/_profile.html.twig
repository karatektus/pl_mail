{#
The profile step's body: just the fields. The wizard shell owns the heading, the
progress rail, the buttons and the form tags — a step template is only ever what
goes between them.
#}
{{ form_widget(form) }}
