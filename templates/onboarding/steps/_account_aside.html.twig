{#
The prose beside the account fields. Deliberately short: someone who knows their
IMAP settings does not need it, and someone who does not needs to be told where
to look rather than taught the protocol.
#}
<p class="font-medium text-ink-soft">{{ 'onboarding.aside.account.heading'|trans }}</p>
<p class="mt-2">{{ 'onboarding.aside.account.body'|trans }}</p>
<p class="mt-3">{{ 'onboarding.aside.account.oauth'|trans }}</p>
