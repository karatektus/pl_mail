{# Google or Microsoft, one at a time, with the same fields the admin panel uses. #}
{% include 'onboarding/steps/_provider_switch.html.twig' with {
    providers: providers,
    provider: provider,
    step: step,
} only %}

{{ form_widget(form) }}
