{# What somebody needs to have ready before this step is answerable, and
   permission to skip it — which is the more important half. #}
<p class="font-medium text-ink-soft">{{ 'onboarding.aside.admin_ai.heading'|trans }}</p>
<p class="mt-2">{{ 'onboarding.aside.admin_ai.body'|trans }}</p>

<div class="mt-4 pt-4 border-t border-line">
    <p class="font-medium text-ink-soft">{{ 'onboarding.aside.admin_ai.skip_heading'|trans }}</p>
    <p class="mt-2">{{ 'onboarding.aside.admin_ai.skip_body'|trans }}</p>
</div>
