{% extends '_layout/app.html.twig' %}

{% block body %}
    <turbo-frame id="modal">
        <template data-ui--modal-frame-title>{{ 'settings.accounts.edit'|trans }}</template>

        {{ form_start(form, {
            attr: {
                'data-turbo': 'true',
                'data-controller': 'imap-preset connection-test',
                'data-settings--connection-test-url-value': path('app_account_test_connection'),
                'data-settings--connection-test-csrf-value': csrf_token('account_test'),
                'data-settings--connection-test-account-id-value': account.id,
                class: 'flex-1 min-h-0 flex flex-col',
            }
        }) }}

        <div class="flex-1 min-h-0 overflow-y-auto px-6 pb-6 space-y-5">
            {% include 'account/_fields.html.twig' with { form: form, mode: 'edit' } %}
        </div>

        <div class="flex items-center justify-end gap-2 px-5 py-3.5">
            <button type="button" data-action="click->ui--modal#close"
                    class="px-3 h-9 rounded-lg text-sm font-medium text-ink-soft hover:bg-hover transition-colors cursor-pointer">
                {{ 'form.cancel'|trans }}
            </button>
            <button type="submit"
                    class="px-4 h-9 rounded-lg text-sm font-medium bg-accent hover:bg-accent-strong text-white transition-colors cursor-pointer">
                {{ 'form.submit'|trans }}
            </button>
        </div>

        {{ form_end(form) }}
    </turbo-frame>
{% endblock %}
