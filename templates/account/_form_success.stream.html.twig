{# Close the modal by emptying the frame #}
<turbo-stream action="update" target="modal">
    <template></template>
</turbo-stream>

{# Inject a toast #}
<turbo-stream action="append" target="toast-region">
    <template>
        {% include '_partials/_toast.html.twig' with {
            message: 'account.added'|trans,
            type: 'success',
        } %}
    </template>
</turbo-stream>

{# Refresh the account list #}
<turbo-stream action="replace" target="account-list">
    <template>
        {% include '_partials/_account_list.html.twig' %}
    </template>
</turbo-stream>
