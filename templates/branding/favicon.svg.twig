{# The tab icon, painted per user — see BrandingController. Geometry is the
   shared mark macro's, so the favicon and the topbar are the same drawing. #}
{% import '_partials/_logo_mark.html.twig' as logo %}
{{ logo.mark(style, 48) }}
