.git/
.github/
.idea/
**/*.log
docker-compose.override.yml
compose.override.yaml
compose.test.yaml
# Local deployment file — contains real secrets, must never reach an image.
truenas.compose.yaml.local
.env.local
.env.*.local
.env.local.php
var/
vendor/
public/assets/
public/bundles/
assets/vendor/
.phpunit.cache/
phpunit.xml
# Test tooling: the image serves the app, it never runs the browser suite.
# playwright/ holds .auth/user.json — a real authenticated session.
node_modules/
playwright/
playwright-report/
blob-report/
test-results/
.dockerignore
Dockerfile
**/Thumbs.db
**/.DS_Store
